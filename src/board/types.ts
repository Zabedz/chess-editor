/** Which colour sits at the bottom of the board. A presentation concept shared
    by the board and the palette; the pure chess core does not know about it. */
export type Orientation = 'white' | 'black'
